# Seahorse MVP runtime configuration example

[storage]
db_path = "./data/seahorse.db"

[embedding]
dimension = 1024

[jobs]
repair_max_retries = 3
repair_batch_size = 1

[api]
host = "127.0.0.1"
port = 8080

[observability]
log_level = "info"
enable_metrics = true
metrics_path = "/metrics"
